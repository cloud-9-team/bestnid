<% devise_error_messages! %>

<h2 class="subtitulos">Cambiar contraseña</h2>
<div class="container-fluid">
<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
  
  <%= f.hidden_field :reset_password_token %>

   	<div class="row" style="padding-top:1em">
		<div class="col-xs-3 col-md-3"></div>
		<div class="col-xs-6 col-md-6"><%= f.label :password, "Nueva contraseña" %></div>
	</div>

	<div class="row">
		<div class="col-xs-3 col-md-3"></div>
		<div class="col-xs-6 col-md-6"><%= f.password_field :password, autofocus: true, autocomplete: "off", :required => "required", class:"form-control"%></div>
	</div>

	<div class="row" style="padding-top:1em">
		<div class="col-xs-3 col-md-3"></div>
		<div class="col-xs-6 col-md-6"><%= f.label :password, "Confirmar nueva contraseña" %></div>
	</div>

	<div class="row">
		<div class="col-xs-3 col-md-3"></div>
		<div class="col-xs-6 col-md-6"><%= f.password_field :password_confirmation, autocomplete: "off", :required => "required", class:"form-control" %></div>
	</div>

	<div class="row" style="padding-top:1em">
		<div class="col-xs-3 col-md-3"></div>
		<div class="col-xs-6 col-md-6"><%= f.submit "Cambiar contraseña" , class:"btn btn-primary" %></div>
	</div>

<% end %>

	<div class="row" style="padding-top:1em">
			<div class="col-xs-3 col-md-3"></div>
			<div class="col-xs-6 col-md-6"><%= render "devise/shared/links" %></div>
	</div>
</div>